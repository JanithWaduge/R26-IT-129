// lib/services/teacher_session.dart
// TODO: Replace with real authenticated user ID once the auth service is integrated.
// For now, this just holds whatever name the teacher types in once per app session.
class TeacherSession {
  static String? teacherId;
}