// lib/services/teacher_session.dart
// TODO: Replace with real authenticated user identity once the auth service is integrated.
// For now, this holds whatever name/email the teacher types in once per app session.
class TeacherSession {
  static String? teacherId;
  static String? teacherEmail;
}