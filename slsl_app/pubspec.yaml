name: slsl_app
description: Sri Lanka Sign Language Recognition App
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  camera: ^0.10.5+9

 
  tflite_flutter: ^0.10.4
  # Camera
 

  # HTTP — Flask server communication
  http: ^1.2.0

  # Permission handling
  permission_handler: ^11.3.0

  # Objective 1 — Voice-to-Sign
  record: ^5.0.4          # Microphone recording
  path_provider: ^2.1.2   # Temp directory for audio files

  # UI
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/slsl_model.tflite
    - assets/sign_data.json